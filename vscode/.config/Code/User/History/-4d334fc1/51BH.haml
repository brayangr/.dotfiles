.flex-column
  = discount_form.text_field :description, class: 'form-control input-sm', min: 0, value: discount.id ? discount.days : nil, placeholder: 0, data: { action: 'focusout->remunerations--salary-payment-drafts--discounts-days-form#submit input->remunerations--salary-payment-drafts--discounts-days-form#enableSubmit', 'remunerations--salary-payment-drafts--discounts-days-form-target': 'daysInput' }
.flex-column{ data: { controller: 'initialize-autonumeric', 'initialize-autonumeric': { 'query-selector-value': "#employee-#{employee.id}-discounts-#{discount.id ? discount.id : 'new'}" }}}
  = discount_form.text_field :amount, value: discount.id ? discount.amount : nil, placeholder: t('views.remunerations.salary_payment_drafts.amount_placeholder'), class: 'form-control input-sm', id: "employee-#{employee.id}-license-#{license.id ? license.id : 'new'}", data: { action: 'focusout->remunerations--salary-payment-drafts--licenses-form#submit input->remunerations--salary-payment-drafts--licenses-form#enableSubmit', autonumeric: currency_field[:autonumeric].merge!({ uFos: true, vMin: 1 }), 'remunerations--salary-payment-drafts--licenses-form-target': 'lastSalaryInput' }
